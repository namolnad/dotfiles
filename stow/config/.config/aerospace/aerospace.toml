# AeroSpace Config — Context-Isolated Workspace Layout
#
# CONTEXTS (binding modes):
#   alt-1 = Personal  (P)    alt-shift-1 = move window to P-T
#   alt-2 = Work      (W)    alt-shift-2 = move window to W-T
#   alt-3 = Loman Group (L)  alt-shift-3 = move window to L-T
#   alt-4 = DML Foundation (D) alt-shift-4 = move window to D-T
#
# PER-CONTEXT WORKSPACES (prefixed by context):
#   alt-t = Terminal    (P-T, W-T, L-T, D-T)
#   alt-b = Browser     (P-B, W-B, L-B, D-B)
#   alt-e = Email       (P-E, W-E, L-E, D-E)
#   alt-c = Calendar    (P-C, W-C, L-C, D-C)
#
# GLOBAL WORKSPACES (shared across all contexts):
#   alt-m = Media       (M)
#   alt-s = System      (S)
#   alt-n = Notes       (N)
#   alt-a = AI          (A)
#   alt-i = IM/Chat     (I)  — Slack, Messages
#   alt-v = Video       (V)  — Zoom
#   alt-r = Tasks       (R)  — Reminders (no alt-shift-r move-to; conflicts with resize mode)
#
# NAVIGATION:
#   alt-h/j/k/l         = Focus left/down/up/right (vim-style)
#   alt-shift-h/j/k/l   = Move window left/down/up/right
#   alt-slash            = Toggle tile layout (horizontal/vertical)
#   alt-comma            = Toggle accordion layout
#   alt-shift-f          = Fullscreen
#   alt-tab              = Last workspace
#   alt-shift-tab        = Move workspace to next monitor
#   alt-shift-r          = Resize mode (h/j/k/l to resize, b to balance)
#   alt-shift-semicolon  = Service mode (esc=reload, r=reset, f=float toggle)

after-login-command = []
after-startup-command = []
start-at-login = true

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

accordion-padding = 30
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'
key-mapping.preset = 'qwerty'

on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = "move-mouse window-lazy-center"

[gaps]
inner.horizontal = 18
inner.vertical =   18
outer.left =       18
outer.bottom =     18
outer.top =        18
outer.right =      18

# =============================================================================
# MODE: main (defaults to Personal context)
# =============================================================================
[mode.main.binding]

# Context switching — lands on context's terminal (home)
alt-1 = ['workspace P-T', 'mode main']
alt-2 = ['workspace W-T', 'mode work']
alt-3 = ['workspace L-T', 'mode loman']
alt-4 = ['workspace D-T', 'mode dml']

# Focus (vim-style)
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-shift-f = 'fullscreen'

# Workspace navigation
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Per-context workspaces (Personal)
alt-t = 'workspace P-T'
alt-b = 'workspace P-B'
alt-e = 'workspace P-E'
alt-c = 'workspace P-C'
alt-shift-t = 'move-node-to-workspace P-T'
alt-shift-b = 'move-node-to-workspace P-B'
alt-shift-e = 'move-node-to-workspace P-E'
alt-shift-c = 'move-node-to-workspace P-C'

# Move window to another context's home
alt-shift-1 = 'move-node-to-workspace P-T'
alt-shift-2 = 'move-node-to-workspace W-T'
alt-shift-3 = 'move-node-to-workspace L-T'
alt-shift-4 = 'move-node-to-workspace D-T'

# Global workspaces
alt-m = 'workspace M'
alt-s = 'workspace S'
alt-n = 'workspace N'
alt-a = 'workspace A'
alt-i = 'workspace I'
alt-v = 'workspace V'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-s = 'move-node-to-workspace S'
alt-shift-n = 'move-node-to-workspace N'
alt-shift-a = 'move-node-to-workspace A'
alt-shift-i = 'move-node-to-workspace I'
alt-shift-v = 'move-node-to-workspace V'
alt-r = 'workspace R'

# Sub-modes
alt-shift-r = 'mode resize'
alt-shift-semicolon = 'mode service'

# =============================================================================
# MODE: work
# =============================================================================
[mode.work.binding]

# Context switching
alt-1 = ['workspace P-T', 'mode main']
alt-2 = ['workspace W-T', 'mode work']
alt-3 = ['workspace L-T', 'mode loman']
alt-4 = ['workspace D-T', 'mode dml']

# Focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-shift-f = 'fullscreen'

# Workspace navigation
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Per-context workspaces (Work)
alt-t = 'workspace W-T'
alt-b = 'workspace W-B'
alt-e = 'workspace W-E'
alt-c = 'workspace W-C'
alt-shift-t = 'move-node-to-workspace W-T'
alt-shift-b = 'move-node-to-workspace W-B'
alt-shift-e = 'move-node-to-workspace W-E'
alt-shift-c = 'move-node-to-workspace W-C'

# Move window to another context's home
alt-shift-1 = 'move-node-to-workspace P-T'
alt-shift-2 = 'move-node-to-workspace W-T'
alt-shift-3 = 'move-node-to-workspace L-T'
alt-shift-4 = 'move-node-to-workspace D-T'

# Global workspaces
alt-m = 'workspace M'
alt-s = 'workspace S'
alt-n = 'workspace N'
alt-a = 'workspace A'
alt-i = 'workspace I'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-s = 'move-node-to-workspace S'
alt-shift-n = 'move-node-to-workspace N'
alt-shift-a = 'move-node-to-workspace A'
alt-shift-i = 'move-node-to-workspace I'
alt-v = 'workspace V'
alt-shift-v = 'move-node-to-workspace V'
alt-r = 'workspace R'

# Sub-modes (return to work context on exit)
alt-shift-r = 'mode resize-work'
alt-shift-semicolon = 'mode service-work'

# =============================================================================
# MODE: loman (Loman Group)
# =============================================================================
[mode.loman.binding]

# Context switching
alt-1 = ['workspace P-T', 'mode main']
alt-2 = ['workspace W-T', 'mode work']
alt-3 = ['workspace L-T', 'mode loman']
alt-4 = ['workspace D-T', 'mode dml']

# Focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-shift-f = 'fullscreen'

# Workspace navigation
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Per-context workspaces (Loman Group)
alt-t = 'workspace L-T'
alt-b = 'workspace L-B'
alt-e = 'workspace L-E'
alt-c = 'workspace L-C'
alt-shift-t = 'move-node-to-workspace L-T'
alt-shift-b = 'move-node-to-workspace L-B'
alt-shift-e = 'move-node-to-workspace L-E'
alt-shift-c = 'move-node-to-workspace L-C'

# Move window to another context's home
alt-shift-1 = 'move-node-to-workspace P-T'
alt-shift-2 = 'move-node-to-workspace W-T'
alt-shift-3 = 'move-node-to-workspace L-T'
alt-shift-4 = 'move-node-to-workspace D-T'

# Global workspaces
alt-m = 'workspace M'
alt-s = 'workspace S'
alt-n = 'workspace N'
alt-a = 'workspace A'
alt-i = 'workspace I'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-s = 'move-node-to-workspace S'
alt-shift-n = 'move-node-to-workspace N'
alt-shift-a = 'move-node-to-workspace A'
alt-shift-i = 'move-node-to-workspace I'
alt-v = 'workspace V'
alt-shift-v = 'move-node-to-workspace V'
alt-r = 'workspace R'

# Sub-modes (return to loman context on exit)
alt-shift-r = 'mode resize-loman'
alt-shift-semicolon = 'mode service-loman'

# =============================================================================
# MODE: dml (DML Foundation)
# =============================================================================
[mode.dml.binding]

# Context switching
alt-1 = ['workspace P-T', 'mode main']
alt-2 = ['workspace W-T', 'mode work']
alt-3 = ['workspace L-T', 'mode loman']
alt-4 = ['workspace D-T', 'mode dml']

# Focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-shift-f = 'fullscreen'

# Workspace navigation
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Per-context workspaces (DML Foundation)
alt-t = 'workspace D-T'
alt-b = 'workspace D-B'
alt-e = 'workspace D-E'
alt-c = 'workspace D-C'
alt-shift-t = 'move-node-to-workspace D-T'
alt-shift-b = 'move-node-to-workspace D-B'
alt-shift-e = 'move-node-to-workspace D-E'
alt-shift-c = 'move-node-to-workspace D-C'

# Move window to another context's home
alt-shift-1 = 'move-node-to-workspace P-T'
alt-shift-2 = 'move-node-to-workspace W-T'
alt-shift-3 = 'move-node-to-workspace L-T'
alt-shift-4 = 'move-node-to-workspace D-T'

# Global workspaces
alt-m = 'workspace M'
alt-s = 'workspace S'
alt-n = 'workspace N'
alt-a = 'workspace A'
alt-i = 'workspace I'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-s = 'move-node-to-workspace S'
alt-shift-n = 'move-node-to-workspace N'
alt-shift-a = 'move-node-to-workspace A'
alt-shift-i = 'move-node-to-workspace I'
alt-v = 'workspace V'
alt-shift-v = 'move-node-to-workspace V'
alt-r = 'workspace R'

# Sub-modes (return to dml context on exit)
alt-shift-r = 'mode resize-dml'
alt-shift-semicolon = 'mode service-dml'

# =============================================================================
# RESIZE MODES (one per context, returns to correct context on exit)
# =============================================================================
[mode.resize.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'
minus = 'resize smart -50'
equal = 'resize smart +50'
enter = 'mode main'
esc = 'mode main'

[mode.resize-work.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'
minus = 'resize smart -50'
equal = 'resize smart +50'
enter = 'mode work'
esc = 'mode work'

[mode.resize-loman.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'
minus = 'resize smart -50'
equal = 'resize smart +50'
enter = 'mode loman'
esc = 'mode loman'

[mode.resize-dml.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'
minus = 'resize smart -50'
equal = 'resize smart +50'
enter = 'mode dml'
esc = 'mode dml'

# =============================================================================
# SERVICE MODES (one per context, returns to correct context on exit)
# =============================================================================
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main']
f = ['layout floating tiling', 'mode main']
backspace = ['close-all-windows-but-current', 'mode main']
alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

[mode.service-work.binding]
esc = ['reload-config', 'mode work']
r = ['flatten-workspace-tree', 'mode work']
f = ['layout floating tiling', 'mode work']
backspace = ['close-all-windows-but-current', 'mode work']
alt-shift-h = ['join-with left', 'mode work']
alt-shift-j = ['join-with down', 'mode work']
alt-shift-k = ['join-with up', 'mode work']
alt-shift-l = ['join-with right', 'mode work']

[mode.service-loman.binding]
esc = ['reload-config', 'mode loman']
r = ['flatten-workspace-tree', 'mode loman']
f = ['layout floating tiling', 'mode loman']
backspace = ['close-all-windows-but-current', 'mode loman']
alt-shift-h = ['join-with left', 'mode loman']
alt-shift-j = ['join-with down', 'mode loman']
alt-shift-k = ['join-with up', 'mode loman']
alt-shift-l = ['join-with right', 'mode loman']

[mode.service-dml.binding]
esc = ['reload-config', 'mode dml']
r = ['flatten-workspace-tree', 'mode dml']
f = ['layout floating tiling', 'mode dml']
backspace = ['close-all-windows-but-current', 'mode dml']
alt-shift-h = ['join-with left', 'mode dml']
alt-shift-j = ['join-with down', 'mode dml']
alt-shift-k = ['join-with up', 'mode dml']
alt-shift-l = ['join-with right', 'mode dml']

# =============================================================================
# AUTO-ASSIGN: Global apps → global workspaces
# =============================================================================
[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = "move-node-to-workspace I"

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = "move-node-to-workspace I"

[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = "move-node-to-workspace V"

[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = "move-node-to-workspace M"

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = "move-node-to-workspace S"

[[on-window-detected]]
if.app-id = 'com.apple.Notes'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = "move-node-to-workspace A"

[[on-window-detected]]
if.app-id = 'com.apple.stocks'
run = "move-node-to-workspace S"

[[on-window-detected]]
if.app-id = 'com.apple.news'
run = "move-node-to-workspace S"

[[on-window-detected]]
if.app-id = 'com.apple.reminders'
run = "move-node-to-workspace R"

# NOTE: Terminals and browsers are NOT auto-assigned because they could
# belong to any context. Place them manually with alt-shift-t / alt-shift-b.
